
import {PasaporteRouter} from './PasaporteRouter'

describe('Pasaporte router Test', () => {
    it('deberia ser un objeto', () => {
        expect(typeof PasaporteRouter()).toBe('object');
      });
})